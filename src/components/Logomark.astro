---
interface Props {
  class?: string;
}
const { class: className = 'h-9 w-9' } = Astro.props;
---

<img src="/images/logo.png" alt="" class={`${className} object-contain`} width="512" height="512" loading="eager" decoding="async" aria-hidden="true" />
